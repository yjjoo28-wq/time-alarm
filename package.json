{
  "name": "jamul-high-school-meals",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "echo 'No lint check needed for static applet'"
  },
  "dependencies": {
    "vite": "^6.2.3"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
